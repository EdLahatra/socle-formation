const LIST = 'LIST';
const UPDATE = 'UPDATE';
const DELETE = 'DELETE';
const CREATE = 'CREATE';
const USER = 'USER';

const url = {
  getInfo: '/api/getUsername',
};

export default {
  LIST,
  UPDATE,
  DELETE,
  CREATE,
  USER,
  url,
};
